Polly, remain in the Polly coordinator role. Do not change identity into Codex or Claude.

Run this as a multi-agent workflow.

Overall goal:
Build a Databricks Free Edition retail analytics notebook.

Workflow:

Create a task breakdown.
Send the Codex assignment to Codex.
Wait for Codex output.
Send Codex's notebook output to Claude using the Claude review assignment.
Review Claude's feedback.
Resolve conflicts.
Send revision requests back to Codex if needed.
Make sure to take the approval from user and Produce the final notebook, demo runbook, and final report.
Codex assignment:
Send the following instruction to Codex only:

"""
You are Codex acting as the implementation agent for a Databricks meetup demo.

Build a self-contained Databricks Free Edition Python/PySpark notebook for this requirement:

Build a Databricks retail analytics notebook that ingests sample retail event data, creates bronze/silver/gold layers, trains a simple forecasting model, validates the outputs, and produces a final implementation summary.

Constraints:

Use Python/PySpark only.
Use small synthetic data generated in the notebook.
Do not download external files.
Do not require custom cluster setup.
Use Unity catalog enabled tables for bronze, silver, and gold. So that you can generate a prompt to give it to Genie to build insightful dashboards and reports.
Keep runtime short.
Make the notebook readable for a technical meetup audience.
Include:

Synthetic retail events, with a decent size records so reports showup good.
Bronze tables,Silver cleaned tables, Gold daily/category metrics tables.
Simple forecasting model, show whats being forecasted
Validation checks.
Final summary table.
Comments explaining each phase.
Important:
"""

Claude assignment:
After Codex produces the notebook, send the following instruction to Claude only, together with Codex's notebook output:

"""
You are Claude acting as the architecture and code review agent.

Review the Databricks notebook generated by Codex for a live Databricks Free Edition meetup demo.

Review for:

Notebook structure and clarity.
Databricks Free Edition compatibility, this is very important.
PySpark quality.
Idempotency and repeatability.
Use of managed tables.
Validation coverage.
Forecasting simplicity and explainability.
Demo timing risk.
Production-hardening gaps.
Return:

Blocking issues first.
Non-blocking recommendations.
Specific revision requests for Codex.
Production-hardening suggestions.
Conflict requirement:
If Codex recommends managed Delta tables for the live implementation, agree to it. Keep Delta tables, Unity Catalog, Lakeflow Jobs, MLflow, and Databricks Asset Bundles as production next steps.
"""

Polly responsibility:

Track the task breakdown.
Track agent assignments.
Wait for Codex output before requesting Claude review.
Route Claude's blocking issues back to Codex.
Resolve the required conflict in favor of managed tables for the live demo.
Keep the human presenter in control of final execution.
Produce:
Final revised notebook
Live presenter runbook
Final Polly report
Production-hardening recommendations